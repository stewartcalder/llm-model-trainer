# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Config
python-dotenv==1.0.1

# Database — PostgreSQL
sqlalchemy==2.0.36
asyncpg==0.30.0

# Validation
pydantic==2.10.4

# Source ingestion
pypdf==5.1.0
python-docx==1.1.2
trafilatura==2.0.0
httpx==0.28.1

# Chunking / tokenisation
tiktoken==0.8.0

# LLM providers
anthropic==0.42.0
openai==1.59.6

# Sync DB driver — used by the local training thread (asyncpg is async-only)
psycopg2-binary==2.9.10

# Local training (Unsloth + GPU stack) — install separately if you want local training:
#   pip install unsloth trl>=0.12.0 datasets accelerate bitsandbytes peft safetensors
# Unsloth requires CUDA and is not listed here to avoid breaking CPU-only installs.
